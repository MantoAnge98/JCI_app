<div id="main" class="container">
 
<h4>Information</h4>
  <table class="table text-center">
    <thead class="thead-light">
      <tr>
        <th>Date du paiement</th> 
        <th>Note / Description</th>
        <th>Montant payé</th> 
        <th>Action</th>
      </tr>
    </thead> 
    <tbody>
      <tr> 
        <td><%= @payment.date_payment %></td>
        <td><%= @payment.note %></td>
        <td><%= @payment.pay_amount %></td> 
        <td>
          <%= link_to "Retour", admin_payments_path, class: "btn btn-secondary" %>
          <%= link_to "Modifier", edit_admin_payment_path(@payment), class: "btn btn-secondary" %>
          <%= link_to "Supprimer", admin_payment_path(@payment.id), method: :delete, data: {confirm: 'Are you sure?'}, class: "btn btn-secondary"%> 
        </td>
      </tr>
    </tbody>  
  </table> 
</div>
